<section id="details-page">
    <h1>Details</h1>
    <article class="details-card">

        <article class="details-card-text">
            <h2>Title:<br> {{publication.title}}</h2>
            <h3>Author: {{username}}</h3>
            <h3>Painting technique: {{publication.paintingTech}}</h3>
            <h3>Certificate of authenticity: {{publication.certificate}}</h3>

            {{#if user}}
            <div class="buttons">
                {{#if isOwner}}
                <a href="/publication/edit/{{publication._id}}" class="btn-edit">Edit</a>
                <a href="#" class="btn-delete">Delete</a>
                {{/if}}
                {{#if isShared}}
                <p class="shared-pub">You already shared this publication</p>
                {{else}}
                <a href="/publication/share/{{publication._id}}" class="btn-share">Share publication</a>
                {{/if}}
            </div>
            {{/if}}

        </article>

        <article class="details-card-image">
            <img src="{{publication.artPicture}}" alt="art-image2">
        </article>

    </article>
</section>